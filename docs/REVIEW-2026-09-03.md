# Super Star Fighter — code, architecture, presentation, and gameplay review

Reviewed September 3, 2026 against the current Beta 10 working tree, including its pre-existing uncommitted changes.

**Remaining-work checklist after batch ten**

Count: **3 distinct work items remain (R24, R25, R27)**. R22 is delivered and human-accepted in batch ten. R12 is conditionally complete on a Logitech G733 in stereo mode, with ordinary-speaker coverage deferred to later human/native-hardware acceptance. R23, R26, and R28 are delivered in batch nine. R28 records all fourteen runtime assets: eleven commercially licensed Ovani Sound tracks, two project-owned visual assets, and one owner-attested public-domain sound. R16–R21 are delivered in batch eight. R11 and R13–R15 were delivered in batch seven. R01–R10 are delivered below. R02/R06/R07 are closed as measurement tools, recorded evidence, and explicit tuning decisions; broader human fairness or release acceptance remains R24/R25. Repeated recommendations are grouped once, and historical findings remain for context.

| ID | Remaining work | Completion target / original review area |
| --- | --- | --- |
| R24 | Human multiplayer/readability acceptance | Validate onboarding comprehension, crowded combat, grayscale/contrast, tactile responsiveness, and perceived fairness with players. Scripted captures do not replace this. |
| R25 | Release performance acceptance | Independently benchmark packaged server and rendered client on representative hardware and run longer full-match/soak coverage with bandwidth, memory trends, and p95/p99. Short local runs are complete. |
| R27 | Native distribution acceptance | Complete clean-machine Windows and native Linux/macOS testing, applicable signing/notarization, and final release artifact checks. Cross-build inspection is insufficient. |

Optional prose suggestions for authenticated reconnect restoration and basic pings remain future scope outside the original 28-item inventory. Accounts, progression, shops, ranked matchmaking, host migration, public matchmaking, and extra card content were explicitly not prerequisites in the review and are not counted. This checklist groups large work packages rather than equal-sized patches.

**Implementation follow-up — tenth batch (R22 prototype)**

Solar Tide now contains **Solar Current**, a visible clockwise annular stream around its central sun. Full influence supplies a modest 0.16 directional input bias and up to an 18% speed advantage when movement aligns with the stream. Normal thrust can oppose or leave it; the field does not damage, stun, drain shields, bend projectiles, or add synchronized entities. Server authority, client prediction, NPC-controlled ships, and the Offline Combat Lab use the same map-authored definition. Field lookup is cached outside the server's 32-ship loop. Network compatibility remains 30 and binary packet version remains 12.

Presentation includes a translucent band, explicit inner/outer boundaries, animated clockwise arrowheads, a text label, stronger high-contrast edges, ship wake feedback, and `SOLAR CURRENT · CLOCKWISE` in the READY countdown. The Offline Combat Lab Targets tab can select **Solar Tide · Solar Current trial** for repeatable evaluation without waiting for match rotation.

Focused coverage passes **23 assertions** for field identity and validation, influence falloff, sustained aligned speed, cap enforcement, opposing control, outward escape, Afterburner interaction, unchanged static-map movement, exact authority/replay agreement, animation lifetime, and high contrast. The complete deterministic suite passes **5,863 assertions**. Foundation passes **173 checks**, including every source/test parse and the deliberately failing exit path. Six-resolution presentation verification passes from 1280×720 through 5120×1440, including dedicated normal/countdown/high-contrast Solar Tide captures; the centered 1080p field and 720p lab compositions were visually inspected.

Three post-optimization Solar Tide overload samples produced p95 values of 24.136, 16.517, and 14.911 ms, a **16.517 ms median** against the 20 ms overload gate. Median movement work was approximately 0.475 ms for all 32 ships versus 0.451 ms in a matching Core Arena sample. The tail failure reinforces R25's representative-hardware requirement. A corrected real-ENet **32-client, 60-second soak passed** with 11 metric windows, maximum full-server p95 5.651 ms, overtime, combat disconnect, late spectator, bounded entities, and clean shutdown. The earlier 90-second game run also completed cleanly with 14 windows and active p95/p99 maxima of 8.303/10.712 ms, but its verifier encountered a null Windows process `ExitCode` after shutdown; the harness now retains and finalizes the native handle, as proven by the passing rerun.

**R22 is complete.** Human acceptance on September 4, 2026 confirmed immediate clockwise/boundary comprehension, approved the graphical treatment, found the speed advantage noticeable but controlled, retained comfortable counter-steering, and accepted its slight tactical route influence. The five-target, 200-HP, shielded, moving, return-fire combat scenario ran well without a readability or performance complaint. Broader telemetry may still justify later strength tuning, but it is not required to close the review item. Movement fields, destructible cover, and teleporter relays are all recorded as future-release improvement priorities in the [Arena Mechanics Roadmap](./ARENA-MECHANICS-ROADMAP.md). The latter two are not implemented in this prototype.

**Implementation follow-up — ninth batch (R23, R26, R28)**

**R12 conditional acceptance:** On a Logitech G733 with Windows mono and Logitech G Surround disabled, combat cues are distinct, left/right shots localize correctly, all objective meanings remain identifiable in the crowded mix, and quality/loudness pass. The initial bilateral result was an operating-system/headset virtualization issue; an experimental pan increase was reverted. No second speaker is available, so ordinary-speaker coverage is deferred to R24/R27 rather than counted as current evidence.

Implemented on top of the uncommitted batch-eight work; protocol 30/binary 12 and gameplay values remain unchanged. Local validation exports are not a new tester-facing beta release.

| ID | Outcome |
| --- | --- |
| R23 | A seeded loopback UDP proxy exercises actual bidirectional ENet delay/jitter, loss, reorder/duplication and a 700 ms blackout. The production client sampler, prediction, projectile presentation, bridge and authoritative world run in a controlled heat. Initial convergence-only checks exposed a missed-press problem: three ability sends could all vanish. Presses now repeat one identity/slot until consumption is acknowledged or 1.25 seconds expires, without extra packets/RPCs. The stricter final profiles require each consumable activation exactly once, ordered acknowledgements, bounded replay, reload/shield/fire coverage, durable resource convergence and predicted-volley cleanup. |
| R26 | All presets use generated resource allowlists before compilation. PCK auditing verifies directory entries, checksums, remap targets and required resources. The Windows dedicated-server export contains 445 entries and 531,638 bytes of game resources, excluding client code/scenes, audio/branding, tests/tools/reports. The official engine binary remains intact. Standalone server startup, rendered client startup and packaged-server 32-client soak pass. Packaging scripts retain engine/component notices and reject stale allowlists. |
| R28 | Source/dependency inventory, fourteen asset records with SHA-256/history evidence, integrity verification, and engine notices covering 102 components/19 named licenses are delivered. Eleven Ovani Sound music files record their commercial embedded-game license; the logo and crosshair are project assets; the existing mine cue is retained under the owner's public-domain attestation. No project-level license is invented. |

Final network matrix: baseline; 100 ms one-way delay ±35 ms jitter; 12% loss; 30% extra 140 ms delay plus 10% duplication; 700 ms blackout; and combined 60 ms ±25 ms, 8% loss, 20% extra delay, 5% duplication and blackout. Actual counters confirm requested faults in both directions. Every final profile delivers one mine, missile and cloak activation, with no repeated spending and final ammo/reload/shot/charge/identity agreement. The fixed build and short seeded sessions do not claim exhaustive WAN fairness or R24/R25 acceptance.

The **32-client, 90-second packaged-server soak passed** with 14 metric windows, four heat results, five overtime events, combat disconnect, late spectator, bounded entities and clean shutdown. Maximum active callback p95/p99: **4.970/6.604 ms**, peak active projectiles: 60. This includes coordination, encoding and ENet enqueue/fan-out; excludes engine transport polling, OS delivery and rendering. The exported engine's static-memory monitor returns zero, meaning unavailable; OS-level memory trends remain R25. Client load agents ran from source. The server executable is unchanged by the subsequent client-only ability-delivery fix.

The audited local Windows client has 553 entries and 19,927,146 resource bytes, including its runtime lab and authored audio; rendered startup reports menu/gameplay/victory inventory 1/4/1. Evidence is under ignored `reports/review-batch9/`, including before/after impairment summaries and `server-soak-summary.json`. Artifacts are under `builds/server/` and `builds/review-validation/`. No commit or publication was requested.

Final foundation: **5,678 assertions passed**, zero failures/script errors; **167 checks** passed including the deliberately failing exit-path test. The ten new regression assertions cover missing/stale/wrapped ability acknowledgements, retry expiry, countdown cancellation, repeated spending and slot-repurposing rejection. All six final real-ENet profiles pass the stricter delivery checks; maximum observed replay queue was 135 of the existing 240-input ceiling. PowerShell parse, allowlist freshness, asset coverage/hash checks and final rendered client startup also pass. Both server notice files are included in the four-file server ZIP.

**Implementation follow-up — eighth batch (R12, R16–R21)**

Implemented on top of `040487c`. Refactors preserve gameplay values, map coordinates and the compatibility-30/binary-12 wire contract. No new arena mechanic or gameplay-balance experiment was introduced.

| ID | Delivered behavior / decision |
| --- | --- |
| R12 | The user listened and reported important sounds remained relatively easy to hear over the crowded mix. This confirms audibility for that listening session. Device, distinct shield/hull recognition and left/right localization were not specified, so those narrower checks remain open; no speculative audio retuning was made. |
| R16 | `NetworkLocalPrediction`, `NetworkReplicatedVisuals`, and `NetworkHudCamera` own input/replay, replicated drawing/interpolation and HUD/camera/spectating state. `NetworkWorldView` retains lifecycle, match state and snapshot ordering with explicit compatibility accessors. Shared `ArenaView`, `CombatShipView`, and `ProjectileLayer` moved from the sandbox directory into presentation with UIDs preserved. No additional scene nodes/render targets or reflective hot-path forwarding. |
| R17 | `NetworkSessionOwner` owns ENet lifecycle, admission/auth throttles, bans, callbacks and teardown; `NetworkReplicationScheduler` owns packet cadence, corrections and outbound accounting. `NetworkBridge` retains all 36 byte-identical RPC signatures/annotations, validated authority boundaries, gameplay integration and the measured outer callback. |
| R18 | Hill and flag handlers own mutable objective state and step/reset logic, returning typed winners and transition snapshots. Coordinator remains the sole heat-transition owner. Intermediate drop/pickup snapshots preserve ordering; resets and observation/contribution accounting match the prior behavior. |
| R19 | Forty-one canonical `CombatStatDescriptor` entries supply names, units, technical bounds, integer/float grouping and benefit polarity. Special-flag mappings also have one source. Derivation/clamping, duplication, card identity, nominal/effective hover text and lab summaries consume the metadata. One modifier-row formatter serves graphical and accessible card details. |
| R20 | Ten explicit `MapDefinition` resources hold map identity, geometry, full-precision spawn anchors, palette and material family. An ordered validated registry replaces map-selection matches and exposes cached read-only cover/circle/palette data; spawn callers receive copies. Invalid dimensions/nonfinite geometry, identity collisions and unsafe spawns are rejected. |
| R21 | Production objective state, step results, transition snapshots and match-event envelopes are typed, with explicit serialization at the existing boundary. `StatChange` values travel directly from derivation to card UI, lab and tutorial. Dictionary APIs remain compatibility adapters; general state/draft/combat payloads and score/build/observation data are intentionally not blanket-converted. |

Before extraction, captured all shipped map geometry, palettes and 320 spawn positions plus full-precision derived-stat hashes for every card at 1/3/20 stacks. The new implementations match those references. An independent old/new objective replay produced byte-identical serialized events across five modes and control/contest/pickup/drop/transfer/reset scenarios.

Four isolated overload runs pass the existing gates. Broken Orbit p95 range **13.691–14.828 ms**, median **14.203 ms**; p99 range **14.866–23.048 ms**, worst max **23.437 ms**. Core Arena p95/p99/max **14.068/17.092/17.974 ms**. Mixed-mine gates also pass. These are current measurements, not a new paired speedup claim; the 23 ms tail still prevents a universal 60 Hz guarantee. Scope includes NPC/world stepping, excluding refill, replication and rendering; R25 retains longer representative-hardware acceptance.

Validation: **5,668 assertions passed**, zero failures/script errors; foundation **165 checks** passed, including the intentionally failing exit path. Full real-ENet network and local-host/LAN lifecycle gates passed. The old/new objective replay adds **42 passing compatibility checks**. All six presentation resolutions passed; inspected 720p lab (including maximum HUD scale), draft/limited-card previews, combat/results, and ultrawide competitive combat captures. Stat text wraps within the existing scrollable lab; shared drawing classes retain combat and objective presentation.

The isolated **32-client, 90-second ENet soak passed**: 14 metric windows, four heat results, five overtime events, authoritative combat disconnect, late spectator, bounded entities and clean shutdown. Maximum active callback p95/p99 was **6.555/8.790 ms**, with 73 peak active projectiles. Timing includes coordination, encoding and ENet enqueue/fan-out, excluding engine transport polling, OS delivery and rendering. This realistic-flow fixture differs from the 1,024-projectile overload benchmark and does not close R25. Summary: `reports/review-batch8/soak-summary.json`.

Evidence is under ignored `reports/review-batch8/`, with objective replay evidence in `reports/r18-compatibility.log`; map/stat reference fixtures are under `tests/fixtures/`. No release export or commit was requested for this batch.

**Implementation follow-up — seventh batch (R11–R15)**

Implemented on top of `e88565f`, with multiplayer performance prioritized. R12 remains explicitly partial until listening acceptance; this batch does not implement the deferred gameplay-balance experiments.

| ID | Delivered behavior / decision |
| --- | --- |
| R11 | Confirmed expanded ultrawide combat visibility. Host-selectable Competitive view freezes a 1920×1080 logical canvas for all standard participants/spectators, with native aspect-preserving bars and no extra render target. Host/operator authority, readiness reset, midmatch locking, rematch retention, late-spectator replication, mouse aim/cursor constraints, resize, and restoration are covered. Casual expanded view remains the default. This is display parity, not a custom-client anti-cheat boundary. |
| R12 | Remote gunfire/effects have separate bounded voice budgets; local shields/hull feedback and objective tones take priority. Spatial panning retains signal in both ears; important cues briefly lower music. Authored explosions/impacts are quieter, master output is limited, and generated weapon-cache entries are capped at 96. Objective baselines avoid replaying initial snapshots. Engine recordings support later listening acceptance; no claim of completed human listening is made. |
| R13 | Existing Godot imports already compress the source WAVs. Retain those formats; discover music by path, load asynchronously on demand, retain only the current context/track, and release inactive music. Resource and process-memory evidence is recorded in the batch evidence notes. |
| R14 | Three paired expensive-map runs target measured collision-query costs. A single occupancy lookup skips empty obstacle cells; a small reusable dense ship-cell table accelerates short sweeps, with sparse fallback for larger/out-of-arena queries. Avoid throwaway arrays when appending to existing cells. Collision ordering/results remain unchanged. Phase-tail reporting now names scope and exclusions. |
| R15 | Draft and standings controllers own their controls, offer/action state, construction, rejection recovery, and reset. A shared card-detail formatter serves both. Root retains navigation/session coordination and explicit compatibility accessors. Fixed five duplicate scoreboard/results overlays and results focus being stolen on each refresh. |

Broken Orbit median p95 improved **15.387 → 14.217 ms (7.6%)** across three isolated runs per version. Before p95 range: **15.248–15.721 ms**; after: **14.163–14.920 ms**. P99 ranges were **16.442–18.004 ms** before and **15.379–19.999 ms** after; there is no stable tail-latency or sustained 60 Hz guarantee. Collision workload counters match. Profiling did not justify changes to reflection/removal logic. The overload fixture includes NPC and world stepping, excluding refill, replication, and client rendering; full callback/ENet validation is separate. Longer representative-hardware acceptance remains R25.

Compatibility is **30**, binary packet version remains **12**; clients and servers must update together for the competitive-view RPC. Native presentation screenshots record the fitted content framebuffer; black window bars lie outside that texture. At 5120×1440 casual view spans 3840×1080 world units; competitive view spans 1920×1080. Resize/camera checks cover six supported resolutions and restore expanded view afterward.

Paired fresh-process audio measurements (three runs per version, warm filesystem caches) reduced median director startup **63.038 → 45.964 ms**, Windows working set **116.83 → 99.83 MB**, and private bytes **67.43 → 50.88 MB**. Startup music buffers fell from **17,761,120 bytes to zero**; entering gameplay retained only the current track (**3,787,800 bytes** in the fixture). These are audio-director process measurements, not full client launch time or a change in packaged asset size. Source WAVs already have compact imported representations; no additional lossy transcode was justified.

Validation: **4,465 assertions passed**, zero failures/script errors; foundation **143 checks** passed including the intentionally failing exit path. All six presentation resolutions passed. Visually inspected the 720p setup/results surfaces and competitive combat capture. The real ENet lobby fixture covers host/guest policy authority, immutable active-match policy, a late spectator, and same-policy fresh rematch. All six resize/camera-world-extent checks pass. Collision regressions compare 4,800 obstacle sweeps and 720 ship sweeps against reference outcomes. Initial integration caught an audio playlist-length regression and a capture assumption that native window bars belonged to the framebuffer; both were corrected before clean gates.

The isolated **32-client, 90-second ENet soak passed**: 14 metric windows, five heat results, overtime, combat disconnect, late spectator, bounded entities, and clean shutdown. Maximum active callback p95 was **5.902 ms**, p99 **7.816 ms**, with 88 peak active projectiles. Scope includes coordination, logging, encoding, and ENet enqueue/fan-out; excludes engine transport polling, OS delivery, and rendering. This realistic-flow fixture is separate from the 1,024-projectile overload benchmark. Immutable local summary: `reports/review-2026-09-03/batch7-soak-summary.json`.

Evidence: `reports/review-2026-09-03/batch7-*.log`, `r14-baseline-*.log`, `r14-after-*.log`, and `.tools/presentation-verification/`. See [audio measurements and audition procedure](./AUDIO-STUDY-2026-09-04.md) for R12/R13 evidence and the remaining listening check. No new release export was produced.

**Implementation follow-up — sixth batch (R01–R10)**

Implemented on top of `08fffb9`. No release export was made for this batch.

| ID | Delivered behavior / decision |
| --- | --- |
| R01 | Stable objective roles choose retrieval, escort, defense, carrier return, and interception. NPCs prioritize public carriers/threats, use ship-clear cover routes with cached graphs and at most two route plans per tick, and warm graphs during countdown. Role/build utility selects useful offered cards. Arrival retains objective behavior; crowded contenders separate and resume fire, fixing a hill combat deadlock exposed by the pacing study. |
| R02 | Bounded authoritative observations and a repeatable five-mode, 2/8/32-pilot study measure actual contact, eliminated time, draft duration, and heat/round turnaround. Post-respawn sampling and round-boundary tests prevent off-by-one and omitted-panel timing errors. Production servers emit `heat_observation` log rows. Findings guide preset recommendations and the objective NPC fix; global timings remain unchanged. |
| R03 | Explained Duel, Skirmish, Team objective, and Chaos presets apply atomically under host authority, fill NPC seats, reset team/readiness settings, retain advanced controls, and refuse to remove connected humans. Locked LAN rows consistently offer Join with clear password status. |
| R04 | A larger roster, explicit scrolling, named missing-ready players, and unready-human-first sorting make the 720p lobby usable. Host configuration lives in a separate scrollable Match Setup modal. |
| R05 | Results report actual hill control/contest time or flag captures, carry time, pickups, and carrier stops. Fresh same-rules rematch resets builds, scores, objectives, observations, inventories, and stale client presentation while preserving current participants/teams/rules. Keep-build extension and lobby return remain distinct; late spectators enter through lobby return. Host-only authority is checked over real ENet. |
| R06 | Production draft availability, 160 mirrored archetype bouts, and 544 saturation comparisons document burst/sustain, shield/multishot, ram/mobility, range/cover, and capped drawbacks. Exact three-card recipes are rare under the sampled draft policy. Card numbers remain unchanged pending player counterplay evidence; warnings and useful-only automatic picks remain appropriate. |
| R07 | 320 controlled next-heat bouts and six five-heat natural matches record builds, byes, and temporary/permanent pickups. Benefits vary by starting build; natural collector counts are too small for a fairness verdict. Keep permanent drops opt-in and quick-preset drops temporary; no speculative catch-up or blanket nerf is added. |
| R08 | Seven stat-derived ship equipment families provide restrained weapon/defense/drive/ordnance/repair shapes inside the existing hull envelope, retaining collision and allegiance markers. |
| R09 | Five cached world material families—station, cargo, crystal, thermal, stone—add distinct seams/ribs/facets/vents/fractures and numbered landmarks on actual solid collision geometry. |
| R10 | Persistent high contrast and independent fire/shield toggles, release-before-rearm lifecycle resets, visible ON/OFF status, scroll/focus-aware settings, and a 21 logical-pixel interface minimum (about 14 physical at 720p). Larger wrapped draft cards, capped warnings, and centered confirmation controls fit the supported layouts. Human readability acceptance remains R24. |

The network contract is now compatibility **29**, binary **12**. New preset/rematch RPCs require clients and servers to update together; binary input/snapshot layouts are unchanged. The repeatable methods, raw-row locations, result tables, and limits are in [the gameplay study](./GAMEPLAY-STUDY-2026-09-03.md).

Validation: **4,328 assertions passed**, zero failures and no script errors. Foundation **132 checks** passed, including its deliberately failing exit-path check. The new localhost ENet fixture verifies host preset replication, guest rejection, fresh rematch replication and resets, and unchanged rules/teams. All six presentation resolutions passed; 720p lobby, equipment families, objective results, high contrast, capped draft, and selected confirmation were visually inspected. Enlarged text revealed real wrapping and centering defects, which were fixed before final capture. Functional navigation tests drive actual NPCs across all ten maps; crowded-hill regression checks reproduce and prevent the no-fire deadlock.

Isolated 32-NPC/1,024-projectile overload checks passed: Core Arena p95 **14.972 ms**, p99 **16.000 ms**; Relay Zero p95 **15.754 ms**, p99 **17.574 ms**. Mixed-mine p95 was **9.370 ms** and **9.261 ms**, respectively. These are defensive development-machine gates; they do not establish release-hardware or every-map 60 Hz acceptance.

The final isolated 32-client ENet run passed for at least **90 seconds**, with **14 metric windows**, four completed heats, overtime, combat disconnect, late spectator admission, bounded entities, and clean shutdown. Maximum active-play p95 was **6.095 ms**, active p99 **7.858 ms**, with 81 peak active projectiles. The callback includes coordination, logging, encoding, and ENet enqueue/fan-out; engine transport polling, OS delivery, and rendering are excluded. The corrected heat summary/cohort logs retained numeric values without script errors. Immutable summary: `reports/review-2026-09-03/batch6-soak-summary.json`.

The first full-client run rejected a numeric-key constructor error in the new telemetry logging. The logger now safely formats numeric keys; heat summaries and per-player cohort rows retain bounded depth and disclose oversized-build truncation. Four additional regressions passed before the clean rerun.

Evidence is under ignored `reports/review-2026-09-03/batch6-*.log` and JSON files; final production captures are under `.tools/presentation-verification/`. No universal balance, WAN, human-readability, or release-hardware acceptance claim is made.

**Implementation follow-up — fifth batch**

The next five priorities are implemented on top of commit `d8acb91`:

| Priority | Delivered behavior |
| --- | --- |
| Guided combat introduction | **Learn to play** starts seven action-based steps in the authoritative lab: movement, confirmed hull hit, completed manual reload, directional shield block, Perfect Guard, selected ability activation, and an effective draft choice. Misses and ordinary held-shield blocks cannot satisfy the wrong step. Retry, restart, skip, controller input, completion, and restoring the prior lab build/targets are covered. The lesson fits at 720p and maximum HUD scale with an accessible fixed action footer. |
| Card identity and draft readability | All 136 cards map from real mechanics/modifiers into 20 visual families. Code-drawn icons and role labels accompany up to three actual before/after changes. Highlights reserve a drawback slot, count omitted tradeoffs, distinguish a new mechanic from a repeat stack, and retain full comparisons and cap explanations in the preview. No card stats or acquisition rules changed. |
| Crowded-combat presentation | Transient effects are bounded to 96, with decorative impacts unable to grow the total past 48 and local damage/death given priority. At 40 effects, filled bursts simplify to outlines. At 160 projectiles, redundant glow and trail length decrease; allied bullets/beams are subdued. Projectile bodies, team shapes, and mine trigger boundaries remain rendered. Terrain outlines are quieter, while objective symbols draw above ordnance. A production fixture renders 32 ships, 1,024 mixed projectiles, flag bases, and mixed combat effects at all six resolutions. |
| Saturated-server optimization | Ship collision bounds are indexed once per tick, allowing most short projectile sweeps to borrow an existing candidate list. Longer sweeps deduplicate/sort candidates, and larger custom radii retain the conservative fallback. Empty ship queries and non-missile guidance avoid unnecessary work. Exhaustive reference comparisons cover short/long sweeps, cell boundaries, equal-hit ordering, and multiple radii. All ten map overload benchmarks passed. |
| Screen ownership | Settings and connection/lobby controllers now own their actual controls, state, handlers, preference storage, binding capture, hosting, LAN discovery, and lobby/appearance behavior. `client_main.gd` retains navigation/match orchestration and explicit compatibility accessors, shrinking from **3,973 to 2,409 lines**. Production host/disconnect, settings, focus, and controller behavior remain covered. |

The network contract remains compatibility **28**, binary **12**; this batch changes neither packet formats nor gameplay authority.

Performance evidence on this machine: Prism Array p95 improved from **17.161 ms** before the index change to **15.249 ms** in the final isolated run; its ship-sweep mean fell from **4.077 ms** to **2.974 ms**. Final Core Arena p95 was **15.116 ms**. The eight other map p95 measurements were Riftline 14.241, Twin Suns 14.595, Dead Freight 15.494, Longwave Array 15.073, Broken Orbit **16.677**, Switchyard 14.764, Solar Tide 14.912, and Relay Zero 15.775 ms. Every run passed the existing overload gate, but Broken Orbit remains just above 16.667 ms and occasional p99/max outliers remain. These are development-machine measurements, not a universal release 60 Hz guarantee. An earlier concurrent Core run missed the mixed-mine gate; final measurements were run sequentially after other tests stopped.

Validation: **4,106 assertions passed**, zero failures and no script errors; foundation **118 checks** passed, including its deliberately failing exit-path check. The two-match/rematch and local-host/LAN gates passed. The first local-host run encountered the concurrently running match server's LAN discovery binding; the isolated rerun passed without changing code or weakening the gate. All six presentation resolutions passed, including tutorial, redesigned draft, and crowded-combat captures. Final objective-layer captures passed at 720p and 5120×1440 and were visually inspected. The fresh 32-client run passed for at least 90 seconds with 14 metric windows, four completed heats, overtime, authoritative combat disconnect, late spectator admission, bounded entities, and clean shutdown. Maximum active-play p95 was **6.190 ms**, active p99 **7.127 ms**, with 48 peak active projectiles. These callback timings include coordination and replication but exclude engine transport polling, OS delivery, and rendering. The short local run is not WAN/loss or release acceptance. Its immutable summary is saved in `reports/review-2026-09-03/batch5-soak-summary.json`.

Render fixtures record forced draw-submission p95, active/drawn projectiles, effects, and suppressed decoration. The 720p fixture draws all **1,024** projectiles while keeping **65** simultaneous effects and suppressing **112** lower-priority additions. This confirms bounded work and retained projectile rendering; submission timing does not measure complete client frame time, GPU completion, or subjective crowded-combat readability. Human playtesting remains necessary.

Evidence is under ignored `reports/review-2026-09-03/batch5-*.log`; captures are in `.tools/presentation-verification/`. The remaining-work checklist above reconciles the outstanding opportunities. No release export was made for this batch.

**Implementation follow-up — fourth batch**

All five subsequently prioritized items are implemented:

| Priority | Delivered behavior |
| --- | --- |
| Independent active abilities | Q/E or controller D-pad left/right selects an owned ability; Shift or left-stick click activates only that selection. Selection is remappable, visible in the HUD, and carried through input encoding, retries, authoritative execution, NPC choices, and prediction replay. A repeated press identity cannot activate a different ability or spend another charge. |
| Authoritative build lab | Replaced the duplicate combat loop with `AuthoritativeWorld`. A paused searchable editor offers five presets, stack add/remove, clear/reset, and configurable target count, hull, distance, shields, fire, and strafe. Measurements report actual shots, hull hits, blocks, hull removed, and elapsed-time DPS including misses/reloads and excluding editor pauses. F2 switches editor/flight; production client event routing is covered. |
| Confirmed combat feedback | Private, coalesced server events distinguish hull damage, blocked shots, Perfect Guard, reflection, and defender guards. Death recaps identify the killer, weapon/environment, actual final hull damage, and relevant shield mechanic. Life generations reject stale recaps and preserve feedback arriving before its snapshot. Actual mine detonation events drive blasts; retirement and budget eviction no longer fabricate explosions. |
| Accessibility and controller interaction | Persistent 100–150% HUD scale, reduced shake, reduced flashes, and a centered 16:9 HUD safe area. The arena retains its wider view. Keyboard/controller settings tabs and sliders are operable; the lab editor scrolls at small resolutions and maximum scale. Feedback is positioned above the outer navigation-marker band. |
| Projectile budgets and server performance | Existing 64-per-owner/1,024-global totals remain. Mines have bounded reservations of 16 per owner/512 globally, and moving fire replaces moving ordnance instead of deleting mines. HUD deployment counts and replacement warnings explain pressure. Added collision-phase profiles, map-selectable benchmarks, active-play p95/p99, coordination/replication phase metrics, and a full server-callback acceptance gate. An empty-cell obstacle-sweep fast path avoids unnecessary collision work. |

Current compatibility is **28**, binary packet version **12**. Input packets are **21 bytes**; the recipient correction trailer is **57 bytes**, leaving a full 32-player snapshot at **1,187 bytes**, below the 1,200-byte target. Clients and servers must update together. Historical version and verification figures in earlier batches below describe those batches only.

Performance evidence: the 32-client ENet run lasted at least **90 seconds**, captured **14 metric windows**, and passed active-play p95 below 16.667 ms, authoritative combat disconnect, late spectator admission, overtime, bounded entities, and clean shutdown. Maximum active-window p95 was **6.363 ms**, p99 **7.962 ms**; peak active projectiles were **59**. Timing covers the server physics callback, including coordination, encoding, and ENet enqueue/fan-out; it excludes engine transport polling, OS delivery, and client rendering. This is a short local acceptance run, not a WAN or release soak.

The instrumented 32-insane-NPC/1,024-projectile Core Arena benchmark measured p95 **18.297 ms** before the empty-cell fast path and **16.876 ms** afterward. Prism Array measured **17.301 ms**; mixed-special Core Arena and Prism workloads measured **11.351 ms** and **10.627 ms** respectively. These pass the existing 20 ms overload gate, but the saturated workload does **not** demonstrate a sustained 60 Hz guarantee. Single-machine before/after measurements are not a controlled statistical study. Collision profiles remain available for further work.

Validation: **3,769 assertions passed**, zero failures and no script errors; foundation **109 checks** passed, including the separate deliberately failing exit-path check. Real ENet network, two-match/rematch, malformed/excessive-client isolation, and the 32-client run passed. Presentation verification passed all six resolutions; new lab, accessibility, ability-selection, hit/block, and death-recap screens were inspected at 720p and ultrawide, including maximum HUD scale. A final camera reset correction makes the lab open immediately on its firing lane; the complete assertion suite and 720p production capture passed again afterward. `git diff --check` and UTF-8/line-ending checks passed.

Added regressions cover selected-only execution, retry deduplication, unacknowledged replay, packet validation/sizes, protected mine quotas and queue compaction, actual versus retired-mine explosion events, authoritative damage/block/death provenance, stale and early death-event ordering, shared lab mechanics/measurements, persisted accessibility preferences, controller settings interactions, and production F2 routing. A new replay fixture initially omitted the ordinary ship snapshot fields when supplying its correction trailer; the test gate rejected it, the fixture was corrected to merge both as production does, and the complete suite passed. Evidence is under ignored `reports/review-2026-09-03/batch4-*.log`, with final results in `batch4-foundation-final.log`, `batch4-tests-final.log`, and `batch4-presentation-final.log`; live-run metrics are in `.tools/soak-verification/summary.json` and screenshots in `.tools/presentation-verification/`.

Remaining opportunities include a guided introduction, broader build/balance playtesting, further presentation and responsibility cleanup, saturated-server optimization, longer WAN/loss acceptance, and release/native-platform validation. No release export was performed.

**Implementation follow-up — third batch**

| Finding / opportunity | Current status |
| --- | --- |
| 10 — Cloak information boundary | Network snapshots omit a cloaked ship from all other recipients, including allies and spectators. Only the owner receives its current ship state and correction trailer. The production client removes hidden ships and interpolation/feedback history; reveal starts at the new authoritative position without interpolating through hidden travel or reporting stale damage. |
| Cloak objective/counter rules | Entering or contesting the hill, picking up the flag, or carrying it reveals cloak explicitly. Activating cloak while doing so consumes the use and reveals immediately. Missile homing, mine magnetism/proximity, collisions, and damage remain unchanged. Public weapons and prior visible positions still provide clues; this is not a claim that every indirect inference is prevented. |
| 5 — Saturated-card acquisition | Manual selection and unlimited ownership remain intact. Cards with no improving effective stat or newly enabled mechanic show **NO EFFECTIVE BENEFIT**, including in confirmation and accessible details. NPC and timeout picks choose among useful offered cards when possible, falling back to the whole offer if none qualify. Drawbacks and caps are unchanged; overflow conversion and wider balance remain future design work. |
| Objective ownership and navigation | Hill boundaries are segmented with a control-progress arc and self/enemy/neutral/contested labels. The HUD distinguishes neutral from contested and tied accumulated leaders. Solo and team bases identify ownership; offscreen hill/flag/base cues include direction and distance, with **RETURN FLAG** for the carrier. Nearby objective markers are separated. Public pilot names are bounded for labels. |
| Static rendering and catalog overhead | `ArenaStaticLayer` owns cached stars, grid, cover, and map labels. Animated overtime/objectives no longer rebuild those drawing commands. Map changes and lab spawn-anchor changes invalidate the static layer. Catalog ID ordering is cached and returned as a defensive copy, invalidated when a card is added. No GPU/frame-time improvement is claimed without a rendering benchmark. |
| Copy and HUD clarity | Replaced `ROUND WINNER BUY` with `ROUND WINNER — SKIPS THIS DRAFT`, expanded pre-overtime shorthand, and put objective status on its own HUD line so it cannot crowd the heat countdown. |
| Documentation drift | Replaced stale current assertion/gate counts in README and developer tables with dated evidence and command-reported counts. Clarified that the map expansion's proposed resource validator and dynamic-mechanic contract are roadmap requirements; historical release evidence remains unchanged. |

This batch introduced compatibility **27**, binary packet version **11**, now superseded by the fourth batch above. Its 32-player owner snapshot was at most 1,181 bytes. Manual, specification, and developer notes document the new rules.

Validation: **3,515 assertions passed**, zero failures and no script errors; foundation **99 checks** passed, including the separate deliberately failing exit-path check. Real ENet network and two-match rematch gates passed. Presentation verification passed all six resolutions; final capped-card, hill ownership/contest, flag navigation, and return captures were visually inspected at 720p and/or super-ultrawide. New regressions cover recipient visibility at 32 players, client disappearance/reappearance, hill/flag reveals, preserved missile targeting, useful automatic choices and all-capped fallback, cache invalidation, base ownership/navigation, neutral/contested state, and tied Hill leaders. Evidence is in ignored `reports/review-2026-09-03/batch3-*.log`; the final full suite result is in `batch3-foundation.log`. An initial new test used an incorrect scene path; the test gate rejected that partial run, the path was corrected, and the complete suite passed.

The fourth batch above addresses active-ability selection, the shared-simulation lab, combat feedback, accessibility settings, projectile budgets, and initial full-server performance acceptance. A guided introduction, further responsibility extraction, and broader balance/visual polish remain. This third batch did not constitute a human multiplayer playtest, WAN-loss soak, release export, or completion of every opportunity in the review.

**Implementation follow-up — second batch**

The next four prioritized interaction gaps are implemented in the working tree:

| Finding | Current status |
| --- | --- |
| 6 — Unbounded heats | Every mode now has a hard deadline 60 seconds after overtime starts (105 seconds by default), with a HUD countdown and time-limit result label. Hill awards a sole positive control-time leader, including a dead pilot awaiting respawn; equal leaders and unresolved other modes draw. Normal objective completion on the deadline takes precedence. Flag overtime preserves every scoring base. Repeated draws can still extend the overall match. |
| 7 — Unsafe respawns | Placement stays inside the safe circle and clear of geometry and living ships, then scores enemy proximity, cover, incoming shots, and mines. It waits when no legal position exists. Threat queries use a shared spatial cache; work is bounded to two attempts per physics tick with a half-second retry delay for blocked pilots. This is safer placement, not invulnerability or a guarantee against future attacks. |
| 8 — Team allegiance | Team numbers, ally/enemy labels, and circle/diamond shapes now distinguish ships, weapons, mines, and offscreen indicators despite identical hull cosmetics. Friendly ordnance no longer triggers incoming-threat warnings. The compact combat HUD explains the shape key; non-team modes retain their existing treatment. |
| 11 — Unbounded powerups | At most eight uncollected pickups exist, each lasting at most 60 seconds and removed if overtaken by overtime. Placement respects the safe circle and skips unavailable locations. Missed intervals cannot create a catch-up burst. Removal is synchronized without granting a card or playing collection feedback. Collected-card permanence is unchanged. |

This batch introduced compatibility **26**, with binary packet version **11** unchanged (superseded by compatibility 27 above). The manual and specification document the new rules.

Validation: **3,458 assertions passed**, zero failures; foundation **97 checks** passed; real ENet network, two-match rematch, and hardening gates passed. Presentation verification passed all six resolutions, with final team-combat captures visually inspected at 720p and super-ultrawide. Tests cover all five modes across all ten maps, deadline boundaries, Hill ties and dead leaders, safe respawn constraints and attempt budgets, pickup expiry/caps, reflected ordnance allegiance, and production client event handling. These checks do not replace human playtesting or a packet-loss soak.

The hardening fixture now sends malformed inputs over a bounded six-second window after the normal reliable ready message. Diagnostic runs confirmed isolation after the server received three malformed packets; the former short unreliable startup burst did not consistently deliver enough packets. Temporary diagnostics have been removed, and the clean harness passed.

A focused 32-query respawn benchmark with 1,024 projectiles improved from **960.872 ms to 189.841 ms** after spatial threat caching. This measures the placement workload, not full-server frame time or production p95. Normal execution additionally limits attempts to two per tick.

The third batch above addresses cloak replication, automatic saturated-card choices, objective navigation, and selected rendering/catalog overhead. Broader opportunities remain below. Evidence is under ignored `reports/review-2026-09-03/batch2-*.log`; screenshots are under `.tools/presentation-verification/`. No release export or human multiplayer playtest was performed.

**Implementation follow-up — first batch**

The original findings below remain the review baseline. The following changes have since been implemented in the working tree:

| Finding | Current status |
| --- | --- |
| 1 — Respawn projectile cleanup | Fixed: cleanup captures existing projectile IDs and checks current ownership; fresh shots and reflected shots survive the earlier owner's timer. |
| 2 — Prediction mismatch | Fixed for the identified movement/ability state: authority and replay share combat-input simulation, including Afterburner, actual shield state, and Breakaway. Snapshots restore ability and shield recovery clocks before replay. Other world interactions, including collisions with moving opponents and incoming damage, still require authoritative correction. |
| 3 — Stale input and repeated special | Fixed: each special press has a stable identity across retries, and human input becomes neutral after 0.5 seconds without a fresh frame. Server-owned NPCs retain their decision cadence. |
| 4 — Fire-rate plateaus | Fixed: fractional tick remainder preserves sustained cadence without accumulating idle/reload credit. |
| 5 — Hidden cap drawbacks | Feedback implemented: draft previews compare the whole current and proposed builds after limits. Cards and confirmation flag limited effects; the remaining damage/spread drawbacks stay visible. Acquisition rules and the underlying balance question are unchanged. |
| 9 — Incomplete weapon correction | Fixed: recipient snapshots carry reload progress, cooldown, shot identity, and life generation. The production renderer clears stale reload/input/volley state on respawn, including when the dead snapshot was lost. |

This batch introduced protocol compatibility **25**, binary packet version **11** (superseded by compatibility 26 above). Inputs are 20 bytes. A 32-player snapshot, including its recipient-specific 51-byte correction trailer, is **1,181 bytes**, below the 1,200-byte transport target. The test command also rejects Godot script errors even when Godot prints a successful partial test summary.

Validation: **3,094 assertions passed**, zero failures and no script errors; foundation **95 checks** passed; real ENet network and two-match rematch gates passed; hardening passed on an unchanged retry after its first run timed out awaiting malformed-client isolation. All six presentation resolutions passed, including two added capped-draft captures per resolution. New captures were visually inspected at 720p, 1080p, and super-ultrawide. Shared replay matched authority exactly in unquantized delayed-state tests; the wire-encoded 300 ms delayed-state case had **0.03119 px** maximum position error. Production-renderer tests covered mid-reload correction and a respawn whose death snapshot was skipped. These are scripted checks, not a human network playtest or packet-loss soak.

The subsequent batches above address findings **6, 8, 7, 11, and 10**, plus acquisition safeguards for finding 5. Wider balance and broader opportunities below remain open.

Implementation evidence is under the ignored `reports/review-2026-09-03/implementation-*.log` files; presentation images are under `.tools/presentation-verification/`.

**Overall assessment.** The project has a strong multiplayer foundation and enough mechanics to support a distinctive game. Its next milestone should concentrate on reliable combat feedback, understandable builds, team/objective readability, and predictable match pacing. More cards and modes would increase the current interaction and maintenance problems before resolving them.

The strongest elements are server-owned combat and drafting, separation of most simulation data from scene nodes, deterministic testability, bounded projectile transport, shared map geometry, configurable bots, and an established visual palette. The weakest areas are simulation parity between online prediction/offline play/server truth, hidden limits on advertised card effects, and a presentation layer that communicates individual statistics better than tactical decisions.

**Evidence and limits**

| Check performed | Result |
| --- | --- |
| Existing deterministic test suite | 2,998 assertions passed; zero failures |
| Full foundation gate | Passed, 94 checks, including parsing source/test scripts and verifying the deliberately failing test path |
| Real ENet network harness | Passed handshake/rejections, lobby authority, input/snapshots, projectiles, leader transfer, and late spectator checks |
| Production presentation harness | Passed all six resolutions; 39 PNGs per resolution, 234 generated captures |
| Visual inspection | Examined menu, lobby, options, settings, controls, draft/preview, combat, offline lab, scoreboard, results, map examples, 3:2 drafting, and super-ultrawide combat |
| Existing overload benchmark | Passed its configured limits; results below |
| Focused review probes | Reproduced respawn projectile deletion, Afterburner prediction mismatch, stale special reactivation, fire-rate plateaus, and detrimental cap-saturated stacking; checked final overtime safe positions on all ten maps |

The presentation gate initially failed because the restricted environment could not write Godot's normal user logs or read the Windows certificate store. It passed when rerun with the required environment access. The standalone test/benchmark/probe runs also emitted environment errors; their assertion/results markers are recorded separately from those errors.

This is a broad source and scripted-runtime review, not a claim of exhaustive path coverage. No human multiplayer session, subjective audio listening session, native Linux/macOS playtest, release-export soak, or controlled WAN latency/loss test was performed. Balance, perceived responsiveness, and fun recommendations below are design judgments to validate with players. Screenshots use scripted fixtures: overlapping objects, unusual team scores, and sparse combat in those fixtures are not evidence that live matches produce the same state.

Raw evidence and the runnable probe script are in [the local review folder](C:/Users/Graphite/Documents/code/SuperStarFighter/reports/review-2026-09-03). That folder is Git-ignored. No production code was changed by this review.

**Prioritized defects and interaction gaps**

P1 means address before a wider competitive playtest. P2 means address in the next hardening/polish iteration. Items explicitly marked design gap or source risk need their intended behavior agreed before implementation.

**1. P1 — Respawn cleanup deletes projectiles from the new life. Confirmed by probe.**

`respawn_peer()` resets a ship and then schedules cleanup for its owner ID. Half a second later, the registry deletes every projectile belonging to that owner, including shots fired after respawning. A probe respawned a pilot, added a fresh projectile, advanced cleanup by 0.5 seconds, and observed that fresh projectile removed. This affects the three respawning objective modes and makes the first post-respawn volley unreliable.

Clean up the old life's projectiles before respawn, or associate deferred cleanup with projectile IDs/a life generation. Verify that old shots expire and fresh shots/mines/missiles survive according to their own rules.

Evidence: [respawn](C:/Users/Graphite/Documents/code/SuperStarFighter/src/shared/combat/authoritative_world.gd:177), [owner-wide cleanup](C:/Users/Graphite/Documents/code/SuperStarFighter/src/shared/combat/projectile_registry.gd:163).

**2. P1 — Afterburner prediction does not simulate the server's movement. Confirmed by probe.**

The server applies Afterburner duration, speed multiplier, and acceleration multiplier. Client prediction always passes a speed multiplier of 1.0 and does not replay Afterburner state. The world view adds an initial impulse outside the prediction buffer, so reconciliation cannot reproduce that activation either. Starting both simulations with the same active boost and velocity produced 61 pixels of separation after 300 ms: server speed 792 versus predicted speed 480. This measures simulation divergence without intervening snapshots, not a measured 61-pixel live visual jump.

Make movement-affecting ability state part of prediction and replay. Also cover shield depletion and Breakaway transitions: prediction currently uses requested shielding and a current Breakaway boolean, rather than the historical state for each replayed frame. Test at representative latency and around activation/depletion boundaries.

Evidence: [prediction motion](C:/Users/Graphite/Documents/code/SuperStarFighter/src/shared/network/client_prediction_buffer.gd:83), [client activation](C:/Users/Graphite/Documents/code/SuperStarFighter/src/client/network/network_world_view.gd:270), [server combatant movement](C:/Users/Graphite/Documents/code/SuperStarFighter/src/shared/combat/combatant_state.gd:100).

**3. P1 — A stale input can keep firing or reactivate consumable specials. Confirmed by probe.**

The authority retains the latest input indefinitely and evaluates its special flag every tick. Cloak has an activation latch; Afterburner, mines, and missiles use cooldowns without equivalent action deduplication. One accepted special packet, followed by no new input for about six seconds, spent all three mine charges in the probe. A temporary client stall or loss of neutral/release packets can therefore keep actions running until transport disconnection or fresh input arrives.

Track server receipt time and neutralize stale continuous controls after a deliberate grace period. Give one-shot specials an action sequence/ID, acknowledging and deduplicating retransmissions. Do not simply edge-detect the existing flag without accounting for its three-send reliability scheme.

Evidence: [world input consumption](C:/Users/Graphite/Documents/code/SuperStarFighter/src/shared/combat/authoritative_world.gd:85), [special retransmission](C:/Users/Graphite/Documents/code/SuperStarFighter/src/client/network/network_world_view.gd:233), [input RPC](C:/Users/Graphite/Documents/code/SuperStarFighter/src/shared/network/network_bridge.gd:809).

**4. P2 — Fire-rate upgrades have invisible plateaus. Confirmed by probe.**

Cooldown subtraction clamps away fractional overshoot; firing resets the timer to a whole new interval. At 60 Hz, requested rates of 15, 16, and 19 all yielded 15 shots/second. A request of 20 yielded 20. The probe replenished ammunition to isolate cadence. The practical rate is approximately `60 / ceil(60 / requested_rate)`, so some incremental fire-rate upgrades do nothing and later upgrades cause a sudden jump.

Carry fractional timing remainder or schedule shots against simulation time, while preserving intentional first-shot and reload behavior. Verify long-run cadence across intermediate rates, not just the base rate and clamp maximum.

Evidence: [weapon timer](C:/Users/Graphite/Documents/code/SuperStarFighter/src/shared/combat/weapon_state.gd:19).

**5. P1 — Unlimited acquisition can produce upgrades with only drawbacks. Confirmed design/feedback gap.**

Inventory stacks are unlimited, but effective stats are tightly capped: six projectiles, 20 shots/second, 600 hull, and other limits. Five Twin Shot stacks already produce six projectiles. A sixth keeps six projectiles, lowers per-projectile damage from 4.20175 to 2.941225, and increases spread from 50 to 60 degrees. The offer remains eligible and its preview still presents the card's nominal stacked modifiers. It does not show the actual before/after build after all cards and clamps.

Preserve unlimited card ownership while displaying derived before/after values and explicit saturation. Consider an overflow benefit or a clearly communicated balancing rule where appropriate. Raising caps blindly would worsen projectile budgets and encoding limits. Audit attainable combinations of different cards, not just high counts of one card. This also gives the draft UI a much more useful job than showing nominal rarity odds.

Evidence: [effective clamps](C:/Users/Graphite/Documents/code/SuperStarFighter/src/shared/cards/stat_system.gd:127), [preview calculations](C:/Users/Graphite/Documents/code/SuperStarFighter/src/client/ui/card_hover_button.gd:110), [unconditional repeat eligibility](C:/Users/Graphite/Documents/code/SuperStarFighter/src/shared/cards/card_catalog.gd:193).

**6. P1 — Overtime does not guarantee a heat ends. Confirmed liveness/design gap.**

Overtime stops shrinking at a positive radius and only damages players outside it. The probe found multiple legal, non-overlapping grid positions with zero overtime damage at an elapsed hour on eight of ten maps. Two passive pilots can occupy such positions indefinitely. Core Arena and Solar Tide returned no safe grid points in this sample because central cover occupies the sampled region; overtime behavior therefore also differs materially by map. This was a positional/logic check, not an hour-long playtest.

Objective modes add another route to indefinite play: respawns continue during overtime and there is no terminal heat deadline. In flag modes, the shrinking safe region is centered on the arena while scoring bases remain near its edges, increasingly penalizing the trip required to win.

Define terminal rules per mode: a hard deadline and tie resolution, final sudden death, an overtime objective, or a deliberate respawn cutoff. Test all maps, contested hills, passive pilots, all players dead, and flag-carrying routes. Avoid resolving objective ties by an undocumented arbitrary peer order.

Evidence: [overtime damage](C:/Users/Graphite/Documents/code/SuperStarFighter/src/shared/combat/overtime_system.gd:63), [respawn loop](C:/Users/Graphite/Documents/code/SuperStarFighter/src/shared/match/authoritative_match_coordinator.gd:491), [flag scoring](C:/Users/Graphite/Documents/code/SuperStarFighter/src/shared/match/authoritative_match_coordinator.gd:603).

**7. P2 — Respawn placement checks occupancy, not danger. Source-confirmed design gap.**

The preferred spawn is reused whenever another living ship is more than 60 pixels away and the geometry is clear. It does not assess enemies' firing lanes, armed mines, nearby projectiles, or overtime damage. No spawn-protection state exists in the combatant model. A legal spawn can therefore be tactically unsafe, especially late in objective heats.

Score a bounded set of candidates using enemy/projectile proximity, line of sight, and the current safe region. If adding protection, make its expiry visible and define whether attacking or touching an objective ends it. Validate spawn camping and repeated overtime deaths with actual matches before choosing durations.

Evidence: [spawn selection](C:/Users/Graphite/Documents/code/SuperStarFighter/src/shared/match/authoritative_match_coordinator.gd:522).

**8. P1 — Team allegiance is not a dependable combat visual. Source and visual finding.**

Ship colour/pattern is personal identity. Projectile colour is selected from weapon-card rarity; shield colour is selected from shield-card rarity. None is a reliable indication of whether an entity is friendly. Offscreen ship markers likewise use personal colour and one of three identity marks. Team names in rosters and scoreboards do not resolve the moment-to-moment question of whom to shoot or dodge.

Keep personal hull customization, then add a separate allegiance layer: team crest/number, consistent ally/enemy outline or nameplate badge, and different friendly/hostile projectile treatment. Use shape as well as colour. Preserve stronger contrast for hostile threats and the local pilot. Validate the system in grayscale and with eight teams, not just a cyan-versus-magenta fixture.

Evidence: [ship appearance](C:/Users/Graphite/Documents/code/SuperStarFighter/src/client/network/network_world_view.gd:1246), [projectile palette](C:/Users/Graphite/Documents/code/SuperStarFighter/src/client/sandbox/sandbox_projectile_layer.gd:13), [offscreen markers](C:/Users/Graphite/Documents/code/SuperStarFighter/src/client/presentation/offscreen_indicator_layer.gd:25).

**9. P2 — Weapon state cannot be fully corrected from snapshots. Source risk; player-visible scenario needs targeted integration validation.**

Snapshots contain ammunition but omit reload progress, fire cooldown, and shot sequence. The client corrects ammunition while its separate `local_weapon` continues its own timers. Respawn resets the ship's presentation weapon but does not reset that separate local predictor in the snapshot path. A death during a long reload or a lost reload input can leave the HUD/predicted firing inconsistent with the server until local timing catches up.

Include the minimal authoritative weapon timing/state required for correction, and explicitly reset/reconcile the local weapon at respawn. Test dying mid-reload, manual reload under packet loss, and mid-heat magazine/reload upgrades.

Evidence: [snapshot record](C:/Users/Graphite/Documents/code/SuperStarFighter/src/shared/network/player_snapshot_codec.gd:74), [snapshot application](C:/Users/Graphite/Documents/code/SuperStarFighter/src/client/network/network_world_view.gd:330), [resource reset](C:/Users/Graphite/Documents/code/SuperStarFighter/src/client/network/network_world_view.gd:571).

**10. P2 — Cloak is a rendering rule, not protected hidden information. Source-confirmed architecture boundary.**

The server encodes one complete player-state body and sends it to every human, including exact cloaked positions and velocities. Ordinary clients hide the ship, but a modified client still receives its location. Separately, NPC target selection ignores cloaked pilots while missile acquisition/guidance does not check cloak. These are two distinct decisions: network secrecy and whether homing weapons counter invisibility.

For trusted friend matches this can be an accepted limitation. Before presenting cloak as competitively secret, implement recipient-aware visibility with explicit reveal/despawn/reappearance behavior. Decide and document cloak interaction with missiles, mines, the flag, and hill occupancy. Do not accidentally remove an intended counter mechanic while fixing visibility.

Evidence: [shared snapshot body](C:/Users/Graphite/Documents/code/SuperStarFighter/src/shared/network/network_bridge.gd:1435), [missile acquisition](C:/Users/Graphite/Documents/code/SuperStarFighter/src/shared/combat/authoritative_world.gd:392), [NPC targeting](C:/Users/Graphite/Documents/code/SuperStarFighter/src/shared/combat/npc_pilot_controller.gd:511).

**11. P2 — Uncollected powerups have no lifetime or population cap. Source-confirmed growth risk.**

Powerups continue spawning until the heat ends, remain in a dictionary until collected, and are sorted/scanned against players each simulation step. In an indefinitely stalled heat, a five-second interval can add 720 entries per hour before collections. They can also spawn outside the current overtime safe region because placement does not consume overtime state.

Add a small active-drop budget, expiry/replacement rules, and safe-region-aware placement. Keep these visible and deterministic. This is a secondary performance risk amplified by the missing terminal heat rule, not a measured memory leak.

Evidence: [spawn storage](C:/Users/Graphite/Documents/code/SuperStarFighter/src/shared/combat/card_powerup_system.gd:72), [collection scan](C:/Users/Graphite/Documents/code/SuperStarFighter/src/shared/combat/card_powerup_system.gd:88).

**Architecture and simplification**

| Area | Assessment and recommended change |
| --- | --- |
| Authority boundary | Keep it. Server-owned RNG, cards, scores, damage, and bounded input are the right basis. Avoid moving decisions into UI to make features easier to implement. |
| Three combat paths | The offline lab uses `CharacterBody2D.move_and_slide()` and physics ray queries; the server uses custom collision sweeps; online movement has another replay path. A lab result is not automatically a server result. Make the lab drive `AuthoritativeWorld` through a local adapter, with presentation consuming its state/events. |
| Screen ownership | `client_main.gd` is 3,803 lines and owns connection forms, settings persistence, bindings, splash, hosting, lobby administration, appearance modals, drafting, HUD text, scoreboard, results, and audio routing. Extract cohesive screen controllers incrementally, starting with settings and connection/lobby flows. Keep a small root that coordinates navigation/session state. |
| World presentation | `network_world_view.gd` is 1,308 lines and combines input sampling, prediction, projectile tracking, HUD, camera, spectator controls, feedback, and resource reconciliation. Split input/prediction, replicated-world presentation, and HUD/camera ownership along those existing boundaries. |
| Network bridge | At 1,601 lines, transport, RPCs, admission, server administration, match lifecycle, replication, and metrics are tightly coupled. Extract admission/session services and replication scheduling while keeping RPC entry points explicit and validated. A generic dispatch framework would obscure important permissions. |
| Mode coordination | The 795-line coordinator mixes draft orchestration, map/spawn assignment, respawns, hill scoring, and flag logic. Extract hill/flag handlers with reset/step/snapshot contracts when fixing their lifecycle rules; preserve one owner for heat transitions. |
| Stat metadata | Adding a stat touches the model, stat lists/clamps, tooltip naming, prediction, transport, docs, and tests. Introduce shared stat descriptors for names, units, bounds, polarity, and display formatting. Keep special behaviors explicit until several truly share a lifecycle. |
| Map data | `ArenaLayout` provides useful shared geometry, but layouts and palettes are hardcoded matches. A small validated map resource/registry would make map changes easier. Do this before dynamic hazards, not as a prerequisite to fixing existing combat. |
| State representation | Dictionaries are practical at serialization boundaries, but repeating string keys across simulation and UI hides schema drift. Use typed local event/state objects for frequently edited contracts, with explicit conversion to transport dictionaries. |
| Documentation | README advertises 2,815 assertions/93 checks; the development guide contains 2,923 and differing gate counts, and protocol 22 versus current code 24. The rarity audit's latest catalog count is 135 versus 136 resources. Generate current counts/version tables from code or label old evidence with dates. Separate delivered map rotation from the future roadmap in `maps.md`. |

Production drawing classes currently live under `sandbox/`, even when the online game depends on them. Rename/move these to a shared presentation location as part of extraction; the offline lab should be one consumer. Keep the simple RefCounted simulation and current resource-based cards. There is no evidence that an ECS rewrite, engine migration, or wholesale language rewrite is justified.

**Performance and resource use**

| Benchmark on this machine | p50 | p95 | p99 | Maximum |
| --- | ---: | ---: | ---: | ---: |
| 32 Insane NPCs / 1,024 projectiles | 15.440 ms | 17.064 ms | 19.371 ms | 23.809 ms |
| 512 mines / 512 moving projectiles | 10.282 ms | 10.997 ms | 15.079 ms | 16.516 ms |

The configured overload gate allows 20 ms p95 in the first case, so this is a pass. It does not establish a stable 60 Hz release server: 60 Hz provides about 16.67 ms, and this fixture times NPC decisions plus world simulation, excluding match coordination, encoding, fan-out, and client rendering. It samples 120 measured ticks after 20 warmup ticks on the default map. A release-mode, full-server soak remains necessary.

The benchmark's average phase breakdown was approximately 10.69 ms projectile work, 2.32 ms NPC decisions, 1.78 ms cleanup/threat-index work, 0.33 ms movement, and 0.21 ms overlaps. Optimize projectile work first. The spatial index, cached obstacle geometry, owner queues, reused snapshot body, and revision-driven UI rows are already worthwhile optimizations.

Recommended work, ordered by evidence:

1. Profile projectile collision subphases and temporary allocations on the most expensive maps, then reduce allocations/candidate work where measured. Inspect linear removal deduplication and reflected-projectile update scans under churn. Keep collision correctness as a gate.
2. Include actual coordination/serialization/fan-out in performance acceptance. At 32 players, the 35-byte player record plus 10-byte header is 1,130 bytes; at 20 Hz to 32 humans, player snapshots alone are about 723 KB/s outbound payload, excluding ENet overhead and projectile traffic. Budget and report these separately.
3. Separate static arena drawing from animated objective/overtime layers. `SandboxArena._draw()` redraws stars, grid, and cover whenever its animated objective/overtime queues a redraw. Static drawing can remain cached while only dynamic overlays update. Measure GPU/draw-call impact before batching all visuals.
4. Budget presentation effects at 32 players: hostile projectiles, the local ship, hit feedback, and objectives should survive effect reduction. Test with mines, beams, shields, and Afterburner together; two-ship screenshots do not establish crowd readability or frame time.
5. Review the projectile eviction policy as gameplay, not only optimization. All weapon forms share the per-owner 64/global 1,024 budgets, so new bullets can evict deployed mines or shorten a long-range build's effective projectile lifetime. Consider separate persistent-ordnance reservations and expose saturation. At six projectiles × 20 shots/second, 64 slots represent only about 0.53 seconds of unconstrained continuous output before evictions, absent hits/reloads.
6. Cache immutable sorted card IDs/rarity buckets and avoid rebuilding full stat/catalog views where revisions already identify changes. This is secondary to measured projectile work.
7. Music source WAVs total about 126 MiB and the audio director loads all gameplay tracks at startup. Import settings already enable compression, so source size is not runtime memory usage. Measure packaged audio size, startup latency, and resident memory before choosing streamed compressed music or lazy loading. Keep short effects preloaded.

**Graphical design language**

The navy surfaces, cyan/magenta accents, consistent rounded panels, amber focus treatment, and non-colour ship patterns form a coherent identity. Keep them. Main controls generally fit and the match-options modal is understandable. The game currently looks like a polished geometric prototype: its distinction comes mostly from palette, outlines, and text rather than a recognizable set of ships, weapons, and environmental materials.

| Gap | Concrete direction |
| --- | --- |
| Small type | Combat labels use 14–17 virtual-pixel fonts; at 720p these are roughly 9–11 physical pixels. Card metadata and preview rows are similarly small. Add text/HUD scaling and establish physical-size minimums for actionable text. Keep secondary details in a dedicated panel. |
| Competing borders | Panels, cards, obstacles, shields, pickups, and feed entries all use bright outlines. Reduce stationary-world emphasis; reserve the strongest luminance and motion for the current decision and immediate threats. |
| Team recognition | Add the independent allegiance grammar described in finding 8. Personal colours and rarity colours cannot carry this role reliably. |
| Objective ownership | An occupied hill turns green regardless of who controls it. Pair neutral/contested/self/enemy state with explicit text or symbols and a control-progress arc. Differentiate hill, pickup, shield, and overtime circles by stroke pattern and symbol. |
| Flag navigation | Solo bases are all labeled `PILOT BASE`; offscreen indicators cover nearby ships and an incoming projectile, not the player's base/flag/hill. Add an unmistakable `YOUR BASE` marker, owner labels, and objective direction/distance cues. |
| Ultrawide HUD | At 32:9, critical health information stays at the extreme left while the pilot is central. Offer a constrained HUD safe area independent of world width. Decide whether extra horizontal combat visibility is an accepted casual advantage or needs a competitive viewport rule. |
| Draft composition | The five-card layout has large unused vertical space while effect text and metadata stay small. Use that space for a short role label, a mechanic icon, and two or three actual build deltas. Put probability detail in the preview. |
| Card identity | Most differentiation is wording and rarity colour; the preview has a category glyph. Give mechanic families recognizable icons first—multishot, piercing, ricochet, repair, burst, mine, cloak—before commissioning 136 unique illustrations. |
| Ship evolution | Builds change many numbers without a comparable hull silhouette change. Add restrained, readable attachments/trails for major archetypes; preserve hitbox clarity. Do not make every numeric stack another overlapping effect. |
| World identity | Layouts differ but use broadly the same solid rectangles/circles/grid. Introduce a small material and landmark vocabulary: cover panels, reactor masses, lane markings, boundaries. Decorative art must agree with collision and stay subordinate to threats. |
| Accessibility | Remapping/deadzones and identity patterns are good. Add reduced shake/flashes, text scale, stronger contrast options, and hold/toggle alternatives where suitable. Validate actual focus/tooltip behavior with keyboard/controller, rather than inferring it from focusable controls. |
| Menu hierarchy | The first view centers server discovery, with the combat lab as an alternate. Add a clear quick solo practice route and a short explanation of match structure. `PASSWORD` as a join-row action could be clearer as `JOIN` with a lock indicator. |
| Lobby density | At 720p only a few roster rows are visible while configuration occupies much of the panel. Separate host settings from the roster, preserve readiness summary, and make scrolling/which pilot is unready obvious. |
| Small copy issues | `ROUND WINNER BUY` should be `BYE` or, preferably, `ROUND WINNER — SKIPS THIS DRAFT`. Avoid shorthand such as `OT` in the novice-facing default HUD. |

The rendered fixture places a powerup on the hill, exposing how easily its labels overlap the objective. This is a useful layout stress case, not proof that production spawning selects that exact overlap. Likewise, the preview capture is manually positioned by the harness; it does not establish live tooltip placement or controller navigation.

**Gameplay depth, onboarding, and balance**

The existing loop has meaningful choices: aim versus movement, firing versus shielding, reload timing, perfect guard, shield rams, ricochets, piercing, beam transformations, escape tools, drafting, and objectives. The most valuable missing mechanics are those that help players understand and deliberately combine these choices.

| Priority | Opportunity | Why it matters / acceptance idea |
| --- | --- | --- |
| First | Guided combat introduction | Teach Relative/Newtonian movement, shield arc and firing exclusion, manual reload, perfect guard, then one draft choice. A first-time player should finish a short exercise without opening the manual. |
| First | A usable build laboratory | The existing lab is valuable but relies on developer-style hotkeys and a separate simulation. Add searchable cards, archetype presets, reset, target controls, actual derived stats, and a shot/damage readout backed by the authority model. |
| First | Actual before/after draft comparison | Show effective damage, cadence, projectiles, reload, mobility, and relevant shield changes; call out unlocked mechanics and saturated benefits. This makes 136 cards manageable without removing variety. |
| First | Deliberate active-ability selection | One Special action currently activates every available Afterburner/mine/missile/cloak component. Escaping can spend ordnance and cloak together. Choose whether combo activation is intentional; otherwise support a compact selectable active slot or separate binds, with controller parity. |
| First | Clear hit/death explanation | Existing damage flashes and kill feed are useful. Add authoritative hit/block confirmation for the attacker and a short death recap identifying weapon/environment, killer, and relevant build. Help players distinguish a miss, a block, a network correction, and burst damage. |
| First | Mode-specific pacing | Resolve finding 6, then measure time to first contact, time dead, time drafting, and time to next round. A 32-player elimination heat and a duel need different pacing expectations. |
| Next | Objective team roles | Bots steer directly toward objectives and choose random draft cards. They lack build-aware drafting and explicit escort/defend/intercept roles. Objective steering also lacks a dedicated path around cover; existing flanking primarily addresses the combat target. Add simple roles and bounded waypoint/navigation support before more difficulty tiers. |
| Next | Spawn and comeback fairness | The winner's next-draft bye is already a comeback mechanic. Measure whether weaker pilots gain useful choices and whether permanent pickups create a countervailing snowball. Track win rate conditioned on build and pickups, not only rarity. |
| Next | Archetype support and counterplay | Tag cards by purpose and measure whether plausible builds can be assembled in ordinary match lengths. Test repair/sustain against burst, shield coverage against multishot, rams against mobility, and range against cover. A random high-rarity draw is not proof of a meaningful choice. |
| Next | Fast solo and party presets | Offer a few explained presets, such as duel practice, small skirmish, team objective, and chaos. Preserve advanced settings underneath. This reduces setup burden without deleting host controls. |
| Next | Rematch quality | Add an obvious same-rules rematch and concise results explaining objective contributions, not just kills/rounds. Distinguish extending the current escalating build from starting a fresh match. |
| Later | Signature arena mechanics | Solar Current now supplies the first telegraphed movement-field prototype. Movement fields, destructible cover, and teleporter relays are all committed future-release improvement priorities; expand or ship each only after clear counterplay, shared simulation, accessibility, and multiplayer performance acceptance. See the [Arena Mechanics Roadmap](./ARENA-MECHANICS-ROADMAP.md). |

Death Match currently means last survivor, while the flag modes use one neutral flag and one return wins a heat. Those are legitimate rules, but names such as `Elimination` and `Neutral Flag` may teach expectations better. The hill moves between rounds, not continuously during a heat; its description should make that timing explicit.

Do not treat accounts, progression, a shop, ranked matchmaking, chat, or an expanded card catalog as prerequisites for making this version good. Reconnect restoration is more immediately valuable for long online matches, but requires an explicit authenticated participant-resumption design. Host migration and public matchmaking are larger separate projects. Basic pings could help objective coordination once ownership/navigation are readable.

Audio already has an authored music inventory, event deduplication, prioritized voices, distance attenuation, and weapon-family synthesis. The remaining audible polish needs listening tests: effect masking at 32 players, distinct shield/hull feedback, objective announcements, and whether direction is understandable. Current effect playback uses ordinary `AudioStreamPlayer` with volume falloff; consider spatial panning for nearby threats after evaluating the mix. Avoid adding more loud cues without priority/ducking rules.

**Verification gaps and release readiness**

The existing suite is a significant asset. Its 2,998 passing assertions nevertheless coexist with reproducible lifecycle and timing defects. Add tests at system boundaries rather than increasing counts with more constant/property assertions.

- Add regression coverage for findings 1–5, including activation retransmission and respawn generation changes.
- Add mode/map liveness scenarios with an explicit maximum test duration, all-dead objective states, final-circle occupancy, and scoring routes through overtime.
- Test real input under delayed/lost/reordered delivery; compare authoritative and predicted ability/weapon state, not just decoded packet fields.
- Add 32-player visual stress fixtures, full mixed builds, longest labels, all active abilities, objective modes, and actual controller focus traversal. Existing screenshot existence/size checks establish rendering, not readability.
- Benchmark the release server and rendered client independently on representative hardware, then a full match across expensive maps. Record p95/p99, over-budget ticks, bandwidth, memory trend, and visibility/effect counts.
- Automate export-content allowlisting: presets use `all_resources` with an empty exclusion filter, so test/development resources need deliberate packaging review. Preserve the server-only path and produce a stripped dedicated-server artifact when distribution becomes a priority.
- Complete clean-machine Windows testing, native Linux/macOS acceptance, signing/notarization as appropriate, and release-mode soak. Cross-build inspection is not native play validation.
- Establish source/asset licensing records and attribution inventory; the repository declares no project license and the notices file covers Godot. This is a release bookkeeping gap, not a finding that any bundled asset is unauthorized.

**Suggested implementation order**

| Pass | Work | Exit condition |
| --- | --- | --- |
| 1 — Combat correctness | Fix respawn cleanup, prediction/weapon reconciliation, stale inputs, and fractional firing cadence | Targeted regressions pass; boost/reload/special behavior remains consistent with induced latency/loss |
| 2 — Explain the build | Add effective before/after draft stats, saturation handling, ability activation policy, and a shared-simulation lab | A player can explain what a pick changes and activate the intended ability without spending unintended charges |
| 3 — Match readability and pacing | Allegiance grammar, objective/base navigation, readable scalable HUD, safe respawns, terminal mode rules | New players can identify threats and win conditions; every tested heat terminates under declared rules |
| 4 — Maintainability and performance | Extract screen/mode owners while touching them; optimize measured projectile work and static redraws | Smaller ownership boundaries; release server/client meet measured budgets with full features enabled |
| 5 — Distinctive polish | Mechanic icons, archetype silhouettes, environmental materials, audio mix, one signature map mechanic | Improvements survive crowded combat, grayscale/readability checks, and human playtests |

The best next deliverable is a focused correctness-and-readability build: reliable respawns and abilities, honest upgrade comparisons, unmistakable team/objective cues, and bounded match pacing. That would make the existing content substantially more valuable.
